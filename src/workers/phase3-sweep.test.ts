import {describe,expect,it} from "vitest";import {sweepEndpoints,sweepPoses,summarizeSweep} from "./sweep-science";
describe("deterministic motion sweep science",()=>{it("includes endpoints with exact monotonic progress",()=>{const f=sweepPoses("closing",31);expect(f[0]!.pose).toEqual(sweepEndpoints("closing")[0]);expect(f.at(-1)!.pose).toEqual(sweepEndpoints("closing")[1]);expect(f[0]!.progress).toBe(0);expect(f.at(-1)!.progress).toBe(1);expect(f.every((x,i)=>i===0||x.progress>f[i-1]!.progress)).toBe(true);});it("is deterministic and uses required signed directions",()=>{expect(sweepPoses("closing",11)).toEqual(sweepPoses("closing",11));expect(sweepEndpoints("left-lateral")[1].lateralMeters).toBeLessThan(0);expect(sweepEndpoints("right-lateral")[1].lateralMeters).toBeGreaterThan(0);expect(sweepEndpoints("protrusive")[1].protrusionMeters).toBeGreaterThan(0);});it("summarizes contact and earliest maximum",()=>{const base={requestedPose:sweepEndpoints("closing")[0],appliedTransform:{translationMeters:{x:0,y:0,z:0},rotationQuaternion:{x:0,y:0,z:0,w:1}},classification:"touching" as const,measurementStatus:"rapier-contact" as const,clearanceMeters:0,contactSamples:[],progress:0};const frames=[{...base,frameIndex:0,contactCount:0,penetrationDepthMeters:0},{...base,frameIndex:1,contactCount:1,penetrationDepthMeters:.2},{...base,frameIndex:2,contactCount:1,penetrationDepthMeters:.2}];expect(summarizeSweep(frames)).toEqual({totalFrameCount:3,firstContactFrame:1,lastContactFrame:2,contactFrameCount:2,maximumPenetrationMeters:.2,maximumPenetrationFrame:1,contactPersistsThroughFinalFrame:true});});});
